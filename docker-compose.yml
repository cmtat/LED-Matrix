services:
  matrix-v2:
    build: .
    container_name: matrix-v2
    ports:
      - "5050:5050"
      - "8080:8080"
    restart: unless-stopped
    environment:
      - PUBLIC_HOST=192.168.1.252